[phases.setup]
nixPkgs = [
  "python311",
  "python311Packages.pip",
  "python311Packages.greenlet"
]

[phases.install]
cmds = [
  "cd backend && pip install --break-system-packages --ignore-installed greenlet -r requirements/base.txt"
]

[start]
cmd = "cd backend && python3 entrypoint.sh"
